/.claude/

# Bí mật — `.env.example` là bản mẫu và ĐƯỢC commit; `.env` là bản thật và không bao giờ commit.
.env
.env.*
!.env.example

# Toolchain
/node_modules/
/.next/
/out/
/dist/
*.tsbuildinfo
yarn-error.log

# Test output
/test-results/
/playwright-report/
/playwright/.cache/

# git worktrees live here (feature-flow section 3); they are checkouts, not content
/.worktrees/
